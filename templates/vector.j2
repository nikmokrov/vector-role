data_dir = "{{vector_data_dir }}"

[sources.syslog]
type = "file"
include = [ "/var/log/syslog" ]
ignore_older = 86400

[sinks.clickhouse]
type = "console"
inputs = [ "syslog" ]
target = "stdout"

  [sinks.clickhouse.encoding]
  codec = "json"
